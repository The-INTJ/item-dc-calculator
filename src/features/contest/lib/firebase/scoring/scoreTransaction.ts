/**
 * Score transaction utilities.
 *
 * The primary vote transaction logic is now in scoresProvider.ts,
 * which uses Firestore transactions directly on the votes subcollection
 * and entry aggregates. This module is retained for any future
 * transaction helpers.
 */

export {};
