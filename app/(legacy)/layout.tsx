import type { ReactNode } from 'react';

export const metadata = {
  title: 'Legacy Item DC Calculator',
  description: 'Original shard DC calculator experience preserved alongside the mixology contest.',
};

export default function LegacyGroupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
